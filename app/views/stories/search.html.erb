<h1>Resultado da busca por '<%= @keyword %>'</h1>

<ul>
  <% @stories.each do |story| %>
    <li>
      <a href="<%= story['url'] %>"><%= story['title'] %></a>
    </li>
  <% end %>
</ul>

<a href="/">Voltar</a>
